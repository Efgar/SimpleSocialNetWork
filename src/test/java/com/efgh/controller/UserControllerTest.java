package com.efgh.controller;

import org.junit.Test;

public class UserControllerTest {

    @Test
    public void addUser() {
        /*String userName = "";
        User returnedUser = UserController.addUser(userName);
        Assert.assertEquals(returnedUser, new User(userName);*/
    }
}
